[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "basicirstd-iwt-tools"
version = "0.1.0"
description = "Внутренние инструменты IWT для BasicIRSTD"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
